using Fitness.Common.Projection;
using Microsoft.EntityFrameworkCore; 

namespace Training.API.Trainings.TrainingProjections
{
    public class TrainingUserProjection //: ReadModelAction<TrainingUser>
    {
        //private readonly DbSet<TrainingUser> _users;
        //public TrainingUserProjection(TrainingContext trainingContext) : base(trainingContext)
        //{
        //    //_users = trainingContext.TrainingUsers;
        //    //Projects<Test>(_trainingDetails, (dbSet, e) => dbSet.Where(_ => _.TrainingId == e.TrainingId), (x, @event) => TrainingDetails.Apply(@event));
        //}
    }
}
